server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/connectusdb
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 5GB
      max-request-size: 5GB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: java.teamworks@gmail.com
    password: qzlq qqhh fhdz ylhk
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: true  # Enable debug mode for logging

connectus:
  secret:
    secret-key: ${JAVA_14_SECRET_KEY:defaultSecretKey}
    issuer: ${JAVA_14_ISSUER:defaultIssuer}

minio:
  url: http://localhost:9000
  access-key: user
  secret-key: password
  bucket-name: business-bucket
  secure: false
