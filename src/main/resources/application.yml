server:
  port: 9090
spring:
  datasource:
    url: jdbc:postgresql://dpg-ct87itpopnds73bu3920-a.oregon-postgres.render.com:5432/connectusauthdb
    username: connectusauthdb_user
    password: QkUuiVGArWwxb9HATS2vgtVTUmlnQBBb
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: java.teamworks@gmail.com
    password: qzlq qqhh fhdz ylhk
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: true

connectus:
  secret:
    secret-key: ${JAVA_14_SECRET_KEY:defaultSecretKey}
    issuer: ${JAVA_14_ISSUER:defaultIssuer}

minio:
  url: https://minio-server-latest-1.onrender.com  # Render tarafından sağlanan public URL
  root-user: ${MINIO_ROOT_USER}  # Çevresel değişken ile kullanıcı adı
  root-password: ${MINIO_ROOT_PASSWORD}  # Çevresel değişken ile şifre
  bucket-name: business-bucket
  secure: true  # HTTPS üzerinden bağlantı sağlanacağı için 'secure' true yapmanız gerekebilir
